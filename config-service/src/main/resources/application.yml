server:
  port: 8888
  
spring:
  cloud:
    config:
      server:
        git:
          default-label: "main"
          uri: https://github.com/dogilla/config-data.git
          search-paths: api      

  security:
    user: 
      name: root
      password: admin 